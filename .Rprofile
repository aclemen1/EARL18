.First <- function() {
  options(repos = c(CRAN = "https://cran.r-project.org"))
  invisible(Sys.setlocale("LC_TIME", "en_DK.UTF-8"))
  try({
    library(modulr)
    options(modulr.ignore_deprecated = TRUE)
  })
}
